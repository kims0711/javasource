<인터페이스> (추상클래스보다 인터페이스를 더 많이 쓰기는 함)
-추상클래스(단 추상화 정도가 높다)
-일반메서드, 멤버변수 가질 수 없음
-상수, 추상 메서드 두 가지만 가질 수 있음 !
-인스턴스 생성 불가능 (new 불가능)
-부모 역할로만 사용 가능 (자손 클래스에서 추상 메서드를 반드시 구현하도록 강요)
-인터페이스 상속 - 인터페이스로만 상속 받을 수 있음 / 다중상속 가능*
*단일상속
: 클래스는 단일상속만 가능
class A extends B{} (이렇게 B 하나만 가능하다는 뜻)

<인터페이스의 장점>
: 표준화가 가능하다
: 서로 관계 없는 클래스들에게 관계를 맺어 줄 수 있다. (관계가 없는 클래스의 공통 부모를 만드는 방식으로)
